<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <title>STO Labs</title>
    <!-- Bootstrap -->
    <link href="css/bootstrap.min.css" rel="stylesheet">
    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.3/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>
  <body>


<div class="container">
  <div class="page-header">
    <h1>STO Lab</h1>
  </div>
  <div class="panel panel-default">
    <div class="panel-heading">Fuel-Devops</div>
    <div class="panel-body">
      <div class="table-responsive" >
	
	<table class="table table-hover">
	  <thead>
	    <tr>
	      <th>Name</th>
	      <th>IP</th>
	      <th>Status</th>
	      <th>Platform</th>
            </tr>
	  </thead>
	  <tbody>
	    {% for env in envs %}
  	    {% if env['state'] %}
	    <tr class="success">
	      {% else %}
            <tr>
	      {% endif %}
	      {% if env['url'] %}
              <td ><a href="{{ env['url'] }}" target="_blank">{{ env['name'] }}</a></td>
	      {% else %}
	      <td>{{ env['name'] }}</td>
	      {% endif %}
              <td>{{ env['ip'] }}</td>
              <td >{{ state_to_friendly(env['state']) }}</td>
	      <td>{{ env['platform'] }}</td>
            </tr>
	    {% endfor %}
	    
	</table>
      </div>
      </div>
      </div>
	{% if unavailable|length > 0 %}
    <div class="panel panel-default">
      <div class="panel-heading">Platforms Offline</div>
      <div class="panel-body">
	<ul class="list list-group">
	  {% for node in unavailable %}
	  <li class="list-group-item">{{ node }}</li>
	  {% endfor %}
	</ul>
      </div>
    </div>
    {% endif %}
  </div>


  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
  </body>
</html>


